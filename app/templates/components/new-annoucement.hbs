{{#if addNewAnnoucement}}
<h1>Create A New Annoucement!</h1>
  <div>
    <form>
      <div class="form-group">
        <label for="annoucement">Enter Your New Annoucement:</label>
        {{input value=annoucement}}
      </div>
      <button {{action 'saveAnnoucement1'}}>Save</button>
    </form>
  </div>
{{else}}
  <button {{action 'showAnnoucementForm'}}>New Annoucement</button>
{{/if}}
